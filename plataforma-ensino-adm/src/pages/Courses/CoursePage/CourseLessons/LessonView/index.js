import { Box } from "@material-ui/core";
import VideoContainer from "./Video";

const LessonView = () => {
    return (
        <Box>
            <VideoContainer/>
        </Box>
    );
}

export default LessonView;